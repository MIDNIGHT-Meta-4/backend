spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wordtoworld
    username: root
    password: cy9764dh
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
